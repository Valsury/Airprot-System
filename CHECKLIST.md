# Чеклист деплоя на Render

## ✅ Шаг 1: Blueprint применён
- [x] Blueprint успешно синхронизирован
- [ ] Backend сервис `airport-system-api` создан и запущен

## 🔍 Шаг 2: Проверка Backend сервиса

1. Откройте Render Dashboard
2. Найдите сервис `airport-system-api`
3. Проверьте статус:
   - ✅ Должен быть "Live" (зеленый индикатор)
   - Если "Build failed" или другая ошибка - проверьте логи

4. **Проверьте health endpoint:**
   - Откройте: `https://airport-system-api.onrender.com/api/health`
   - Должен вернуться JSON: `{"status":"OK","message":"Server is running"}`
   - Если ошибка - проверьте логи в Render Dashboard

## 🗄️ Шаг 3: Создание PostgreSQL базы данных

1. В Render Dashboard нажмите **"New +"** → **"PostgreSQL"**
2. Настройки:
   - **Name:** `airport-db`
   - **Database:** `airport_db`
   - **User:** `airport_user`
   - **Region:** Выберите тот же регион, что и backend
   - **PostgreSQL Version:** Любая (например, 15)
   - **Plan:** ⚠️ **ОБЯЗАТЕЛЬНО выберите "Free"**
3. Нажмите **"Create Database"**
4. Дождитесь создания (1-2 минуты)
5. **Важно:** Скопируйте **"Internal Database URL"** (не External!)

## ⚙️ Шаг 4: Настройка Backend с базой данных

1. Откройте сервис `airport-system-api` в Render Dashboard
2. Перейдите на вкладку **"Environment"**
3. Нажмите **"Add Environment Variable"**
4. Добавьте:
   - **Key:** `DATABASE_URL`
   - **Value:** вставьте скопированный Internal Database URL из PostgreSQL
5. Сохраните изменения
6. **Важно:** После добавления переменной Render автоматически перезапустит сервис
7. Подождите 1-2 минуты, пока сервис перезапустится

## ✅ Шаг 5: Проверка Backend с базой данных

1. Снова проверьте health endpoint:
   - `https://airport-system-api.onrender.com/api/health`
   - Должен вернуться: `{"status":"OK","message":"Server is running"}`

2. **Проверьте создание таблиц:**
   - Backend автоматически создаст таблицы при первом запуске
   - Проверьте логи в Render Dashboard → Logs
   - Должна быть строка: `Database initialized successfully`
   - Должна быть строка: `Default admin user created: username=admin, password=admin123`

## 🎨 Шаг 6: Создание Frontend (Static Site)

1. В Render Dashboard нажмите **"New +"** → **"Static Site"**
2. Подключите ваш Git репозиторий (тот же, что и для backend)
3. Настройки:
   - **Name:** `airport-system-app`
   - **Branch:** `main` (или `master`)
   - **Root Directory:** оставьте пустым
   - **Build Command:** `cd client && npm install && npm run build`
   - **Publish Directory:** `client/build`
4. Нажмите **"Advanced"** и добавьте Environment Variable:
   - **Key:** `REACT_APP_API_URL`
   - **Value:** `https://airport-system-api.onrender.com` (без `/api` в конце!)
5. Нажмите **"Create Static Site"**
6. Дождитесь билда (3-5 минут)

## ✅ Шаг 7: Проверка Frontend

1. После завершения билда откройте URL вашего static site
2. Должна открыться страница входа
3. Попробуйте войти:
   - **Username:** `admin`
   - **Password:** `admin123`
4. Если логин успешен - вы должны увидеть Dashboard

## 🐛 Если что-то не работает

### Backend не запускается
- Проверьте логи в Render Dashboard
- Убедитесь, что `DATABASE_URL` правильно скопирован (Internal URL!)
- Проверьте, что база данных создана на Free плане

### Frontend не подключается к Backend
- Проверьте, что `REACT_APP_API_URL` = `https://airport-system-api.onrender.com` (без `/api`)
- Убедитесь, что backend работает (проверьте `/api/health`)
- Откройте консоль браузера (F12) и проверьте ошибки CORS

### Ошибка авторизации
- Убедитесь, что база данных инициализирована
- Проверьте логи backend на наличие ошибок
- Попробуйте пересоздать admin пользователя через API (требует дополнительной настройки)

### База данных "спит"
- На бесплатном плане PostgreSQL может "засыпать" после неактивности
- Первый запрос может занять 30-60 секунд
- Это нормально для бесплатного плана

## 🎉 Готово!

После успешной проверки всех шагов ваше приложение готово к использованию!

**Важно:** Смените пароль администратора после первого входа!

