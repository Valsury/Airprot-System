# Airport Management System

Веб-приложение для управления клиентами и билетами аэропорта. Разработано для учебных целей.

## Возможности

- 🔐 Авторизация сотрудников (JWT)
- 👥 Управление базой клиентов (CRUD)
- 🎫 Управление билетами (CRUD)
- 🔒 Безопасность (хеширование паролей, валидация, CORS, Helmet)
- 📱 Современный адаптивный UI
- 🔍 Поиск и фильтрация данных
- 📄 Пагинация

## Технологии

### Backend
- Node.js + Express
- PostgreSQL
- JWT для авторизации
- bcryptjs для хеширования паролей
- express-validator для валидации
- Helmet для безопасности
- express-rate-limit для защиты от DDoS

### Frontend
- React
- React Router
- Axios для API запросов
- React Hot Toast для уведомлений
- date-fns для работы с датами

## Установка и запуск

### Требования
- Node.js (v16+)
- PostgreSQL
- npm или yarn

### Шаги установки

1. Клонируйте репозиторий или скачайте файлы

2. Установите зависимости:
```bash
npm run install-all
```

3. Настройте базу данных PostgreSQL и создайте файл `server/.env`:
```env
PORT=5000
NODE_ENV=development

DATABASE_URL=postgresql://username:password@localhost:5432/airport_db

JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_EXPIRES_IN=24h

FRONTEND_URL=http://localhost:3000
```

4. Инициализируйте базу данных (таблицы создаются автоматически при первом запуске)

5. Запустите приложение:
```bash
# Запуск в режиме разработки (backend + frontend)
npm run dev

# Или отдельно:
npm run server  # Backend на http://localhost:5000
npm run client  # Frontend на http://localhost:3000
```

## Учетные данные по умолчанию

После первого запуска создается администратор:
- **Username:** admin
- **Password:** admin123

⚠️ **ВАЖНО:** Смените пароль администратора в продакшене!

## Деплой на Render

### Подготовка

1. Убедитесь, что все зависимости установлены локально
2. Создайте PostgreSQL базу данных на Render (бесплатный план)
3. Получите DATABASE_URL от Render

### Настройка на Render

#### Backend Service

1. Создайте новый **Web Service** на Render
2. Подключите ваш Git репозиторий
3. Настройки:
   - **Build Command:** `cd server && npm install`
   - **Start Command:** `cd server && npm start`
   - **Environment:** Node
4. Добавьте Environment Variables:
   - `DATABASE_URL` - из вашей PostgreSQL базы данных
   - `JWT_SECRET` - случайная строка (можно сгенерировать)
   - `NODE_ENV=production`
   - `FRONTEND_URL` - URL вашего frontend (после деплоя)
   - `PORT` - обычно Render автоматически устанавливает

#### Frontend Service

1. Создайте новый **Static Site** на Render
2. Настройки:
   - **Build Command:** `cd client && npm install && npm run build`
   - **Publish Directory:** `client/build`
3. Добавьте Environment Variable:
   - `REACT_APP_API_URL` - URL вашего backend сервиса на Render

#### Альтернатива: Монолитный деплой

Если хотите деплоить как один сервис:

1. Создайте **Web Service**
2. **Build Command:** `npm run install-all && cd client && npm run build`
3. **Start Command:** `cd server && npm start`
4. Обновите `server/index.js` чтобы отдавать статические файлы:

```javascript
// В server/index.js после всех маршрутов, перед error handler:
if (process.env.NODE_ENV === 'production') {
  app.use(express.static(path.join(__dirname, '../client/build')));
  app.get('*', (req, res) => {
    res.sendFile(path.join(__dirname, '../client/build/index.html'));
  });
}
```

5. Установите `path` модуль: `npm install path`

## Структура проекта

```
airport-system/
├── server/
│   ├── db/
│   │   └── init.js          # Инициализация БД
│   ├── middleware/
│   │   └── auth.js          # JWT middleware
│   ├── routes/
│   │   ├── auth.js          # Авторизация
│   │   ├── clients.js       # Клиенты API
│   │   └── tickets.js       # Билеты API
│   ├── index.js             # Точка входа сервера
│   └── package.json
├── client/
│   ├── public/
│   ├── src/
│   │   ├── components/      # React компоненты
│   │   ├── context/         # Auth context
│   │   ├── pages/           # Страницы приложения
│   │   └── App.js
│   └── package.json
└── package.json
```

## API Endpoints

### Авторизация
- `POST /api/auth/login` - Вход
- `POST /api/auth/register` - Регистрация (опционально)

### Клиенты (требуется авторизация)
- `GET /api/clients` - Список клиентов (с пагинацией и поиском)
- `GET /api/clients/:id` - Получить клиента
- `POST /api/clients` - Создать клиента
- `PUT /api/clients/:id` - Обновить клиента
- `DELETE /api/clients/:id` - Удалить клиента

### Билеты (требуется авторизация)
- `GET /api/tickets` - Список билетов (с пагинацией и фильтрами)
- `GET /api/tickets/:id` - Получить билет
- `POST /api/tickets` - Создать билет
- `PUT /api/tickets/:id` - Обновить билет
- `DELETE /api/tickets/:id` - Удалить билет

## Безопасность

- Пароли хешируются с помощью bcryptjs
- JWT токены для авторизации
- Валидация всех входных данных
- CORS настроен для защиты
- Helmet для защиты заголовков
- Rate limiting для защиты от DDoS
- SQL injection защита через параметризованные запросы

## Лицензия

Учебный проект

## Поддержка

Для вопросов и предложений создайте issue в репозитории.
