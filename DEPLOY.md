# Инструкция по деплою на Render

Этот документ содержит подробные инструкции по развертыванию Airport Management System на Render.

## Вариант 1: Полуавтоматический деплой через Render Blueprint

**Важно:** Render Blueprint поддерживает только web services, поэтому через Blueprint создаётся только backend. PostgreSQL и Frontend нужно создать вручную.

1. Загрузите код в Git репозиторий (GitHub, GitLab, Bitbucket)
2. **Сначала создайте PostgreSQL базу данных вручную** (см. Шаг 1 в Варианте 2)
3. Войдите в Render Dashboard
4. Нажмите "New +" → "Blueprint"
5. Подключите ваш репозиторий
6. Render автоматически создаст только **backend сервис** на основе `render.yaml`
7. **После создания backend:**
   - Добавьте `DATABASE_URL` в environment variables backend сервиса (скопируйте Internal Database URL из PostgreSQL сервиса)
   - Создайте **Frontend Static Site вручную** (см. Шаг 3 Вариант А в Варианте 2)

## Вариант 2: Ручной деплой (рекомендуется для первого раза)

### Шаг 1: Создание PostgreSQL базы данных
**7Xt0N>]2QXVO** - password db
1. В Render Dashboard нажмите "New +" → "PostgreSQL"
2. Настройки:
   - **Name:** airport-db
   - **Database:** airport_db
   - **User:** airport_user
   - **Region:** Выберите ближайший
   - **Plan:** Free (для учебного проекта)
3. Сохраните **Internal Database URL** (понадобится для backend)

### Шаг 2: Деплой Backend

1. Нажмите "New +" → "Web Service"
2. Подключите ваш Git репозиторий
3. Настройки:
   - **Name:** airport-system-api
   - **Environment:** Node
   - **Build Command:** `cd server && npm install`
   - **Start Command:** `cd server && npm start`
   - **Plan:** Free
4. Environment Variables:
   ```
   NODE_ENV=production
   DATABASE_URL=<скопируйте из PostgreSQL сервиса>
   JWT_SECRET=<сгенерируйте случайную строку, минимум 32 символа>
   FRONTEND_URL=https://your-frontend-name.onrender.com
   PORT=10000
   ```
5. Сохраните и дождитесь деплоя

### Шаг 3: Деплой Frontend

#### Вариант А: Static Site (отдельный сервис)

1. Нажмите "New +" → "Static Site"
2. Подключите ваш Git репозиторий
3. Настройки:
   - **Name:** airport-system-app
   - **Build Command:** `cd client && npm install && npm run build`
   - **Publish Directory:** `client/build`
4. Environment Variable:
   ```
   REACT_APP_API_URL=https://airport-system-api.onrender.com
   ```
5. Сохраните и дождитесь билда

#### Вариант Б: Монолитный деплой (один сервис)

Если вы хотите деплоить всё как один сервис:

1. Нажмите "New +" → "Web Service"
2. Подключите ваш Git репозиторий
3. Настройки:
   - **Name:** airport-system
   - **Environment:** Node
   - **Build Command:** `npm run install-all && cd client && npm run build`
   - **Start Command:** `cd server && npm start`
   - **Plan:** Free
4. Environment Variables:
   ```
   NODE_ENV=production
   DATABASE_URL=<из PostgreSQL>
   JWT_SECRET=<случайная строка>
   PORT=10000
   ```
5. Сохраните

**Преимущество:** Один URL для всего приложения

**Недостаток:** При изменении frontend нужно пересобрать весь сервис

## После деплоя

1. Откройте ваш сайт на Render
2. Войдите с учетными данными по умолчанию:
   - Username: `admin`
   - Password: `admin123`
3. **ВАЖНО:** Сразу смените пароль администратора (или создайте нового пользователя через /api/auth/register)

## Проверка работоспособности

1. Backend health check: `https://your-api.onrender.com/api/health`
2. Frontend должен загружаться и показывать форму входа
3. После входа должен работать dashboard

## Проблемы и решения

### База данных не подключается
- Убедитесь, что используете правильный `DATABASE_URL` из PostgreSQL сервиса
- Проверьте, что база данных не "заснула" (на бесплатном плане Render БД засыпает после неактивности)

### CORS ошибки
- Убедитесь, что `FRONTEND_URL` в backend соответствует реальному URL frontend
- Если деплоите монолитом, CORS не нужен (можно удалить эту настройку)

### Frontend не видит API
- Проверьте `REACT_APP_API_URL` в environment variables
- Убедитесь, что переменная начинается с `REACT_APP_`
- После изменения переменных пересоберите frontend

### База данных "спит"
- На бесплатном плане Render PostgreSQL засыпает после 90 дней неактивности
- При первом запросе может быть задержка ~30 секунд на пробуждение
- Для продакшена рекомендуется платный план

## Бесплатный план Render - ограничения

- **Спин-даун:** Сервисы "засыпают" после 15 минут неактивности
- **Холодный старт:** Первый запрос после спин-дауна может занять 30-60 секунд
- **База данных:** PostgreSQL засыпает после 90 дней неактивности
- **Лимиты:** 750 часов работы в месяц (достаточно для учебного проекта)

## Обновление приложения

1. Сделайте изменения в коде
2. Закоммитьте и запушьте в Git
3. Render автоматически определит изменения и запустит новый деплой
4. Дождитесь завершения деплоя (обычно 3-5 минут)

## Бэкап базы данных

Для учебного проекта бэкап не критичен, но если нужно:

1. В PostgreSQL сервисе на Render нажмите "Connections"
2. Используйте pg_dump для экспорта данных
3. Или используйте встроенные инструменты Render

---

**Удачи с деплоем!** 🚀
